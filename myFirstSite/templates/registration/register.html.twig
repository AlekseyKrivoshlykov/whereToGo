{% extends 'main/base.html.twig' %}

{% block title %}Регистрация
{% endblock %}


{% block body %}
	{# <div>{{ message }}</div> #}
	<div class="container-fluid">
		<div class="row no-gutters justify-content-center">
			<div class="col col-lg-6 col-md-8">
			<div class="form-login-register shadow-lg">
				{% for flashError in app.flashes('verify_email_error') %}
					<div class="alert alert-danger" role="alert">{{ flashError }}</div>
				{% endfor %}
				<h3 class="mb-3 font-weight-normal title-form-login-register">Регистрация</h3>

				{{ form_start(registrationForm) }}
				{{ form_row(registrationForm.email, {
					label: 'Email:'
				}) }}
				{{ form_row(registrationForm.plainPassword, {
            		label: 'Пароль:'
        		}) }}
				{{ form_row(registrationForm.agreeTerms) }}
				<button type="submit" class="btn btn-success btn-login-register">Регистрация</button>
				{{ form_end(registrationForm) }}
				</div>
			</div>
		</div>
	</div>


{% endblock %}
