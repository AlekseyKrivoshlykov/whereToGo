{% extends 'main/base.html.twig' %}

{% block title %}Горячие источники Камчатки{% endblock %}
{% block body %}
	<div class="container-fluid">
		<div class="row no-gutters justify-content-center mt-5">
			<div class="col-12 col-lg-6 col-md-6">
				<h1 class="text-center title-page">
					{{ title }}
				</h1>
			</div>
		</div>
	</div>
	<div class="container-fluid">
		<div class="row no-gutters justify-content-center mt-5">
			<div class="col-12 col-lg-6 col-md-6 content-text">
				Горячие источники Камчатки - это прекрасные места для отдыха. Только представьте: горячая ванна
				прямо из недров земли.
			</div>
		</div>
	</div>
	{# <ul class="list-group"> #}
	<div class="container-fluid">
		<div class="row no-gutters mt-5">
			{% for spring in hotSprings %}
				{# <li class="list-group-item"> #}
				<div class="col place-card-wrapper">
					<div class="card border-dark mb-3">
						<a href="{{ path('hot_springs_id', {'id': spring.getId }) }}" class="link place_link">
							<img src="{{ asset('assets/main/images/HotSprings/')}}{{ spring.getMainPhoto }}" class="card-img-top" alt="hotspring">
							<div class="card-body text-dark">
								<h5 class="card-title">{{ spring.title}}</h5>
							</div>
						</a>
					</div>
				</div>
				{# </li> #}
			{% endfor %}
		</div>
		{# </ul> #}
	</div>
{% endblock %}
