{% extends 'main/base.html.twig' %}


{% block body %}
	<div class="container-fluid">
		<div class="row no-gutters justify-content-center mt-5">
			<div class="col-12 col-lg-6 col-md-6">
				<h1 class="display-4 text-center">
					Горячие источники
				</h1>
			</div>
		</div>
	</div>
	{# <ul class="list-group"> #}
	<div class="container">
		<div class="row no-gutters justify-content-center mt-5">
			{% for spring in hotSprings %}
				{# <li class="list-group-item"> #}
				<div class="col">
					<div class="card border-dark mb-3" style="width: 18rem; height: 20rem;">
						<a href="#" class="link">
							<img src="{{ asset('assets/main/images/HotSprings/')}}{{ spring.getMainPhoto }}" class="card-img-top" alt="hspring" width="100%" height="200">
							<div class="card-body text-dark">
								<h5 class="card-title">{{ spring.title}}</h5>
							</div>
						</a>
					</div>
				</div>
				{# </li> #}
			{% endfor %}
		</div>
		{# </ul> #}
	</div>
{% endblock %}
